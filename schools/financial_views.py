# Financial views are handled by the dedicated `financial` app.
# This file previously had broken imports (models don't live in `schools`).
# All financial endpoints are now served from financial/views.py at /api/financial/
